import "./Profile.css";

export const Profile = () => {
    const [profile, setProfile] = useState(null);

    return (
        <div className="profileDesign">
            This is Profile
        </div>
    );
}